# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build shared-middleware`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t shared-middleware`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system shared-middleware && \
          adduser --system -G shared-middleware shared-middleware

COPY dist/apps/shared-middleware shared-middleware/
RUN chown -R shared-middleware:shared-middleware .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix shared-middleware --omit=dev -f install

CMD [ "node", "shared-middleware" ]
